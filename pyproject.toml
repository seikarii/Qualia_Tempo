[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "QLA"]

[tool.ruff.external]
command = "python"
args = ["-m", "qualia_code_linter", "--format", "ruff", "--stdin-filename", "{filename}"]