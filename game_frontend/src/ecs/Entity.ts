export type Entity = number;

// This function is intended for testing purposes only to ensure a clean state.
export function _resetEntityIdForTests(): void {
  // In a real scenario, you might need to reset ECSManager's nextEntityId as well.
  // For now, this is a placeholder for test-specific resets.
}
